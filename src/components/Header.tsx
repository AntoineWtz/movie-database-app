import React from 'react';

const Header: React.FC = () => {
    return (
        <header className="bg-blue-500 py-4">
            <h1 className="text-4xl font-bold text-white text-center">Movie Database</h1>
        </header>
    );
};

export default Header;
